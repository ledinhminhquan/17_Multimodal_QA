# Full local/dev install (everything). For a minimal core install use `pip install -e .`
# and add extras: `.[ml]`, `.[api]`, `.[report]`, or `.[all]`.

# -- core (package + SceneStubVQA + synthetic scenes + VQA-accuracy metric + agent + tests) --
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
Pillow>=9.5

# -- trainable VQA core (ViLT / BLIP) --
torch>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.1.99

# -- serving (API + UI) --
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
python-multipart>=0.0.9

# -- reporting (auto report.pdf + slides.pptx) --
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7

# -- optional --
# anthropic>=0.39          # optional LLM review-note brain for the agent
